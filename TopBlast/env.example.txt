# ============================================
# TOPBLAST - Robinhood Chain (EVM) Configuration
# ============================================
# Same env key names as Solana deployment — swap values only
# Solana build preserved on branch: sol-build
# ============================================

# ============================================
# DATABASE (Required)
# ============================================
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/topblast?retryWrites=true&w=majority

# ============================================
# RPC (Optional override)
# ============================================
# Default: https://rpc.mainnet.chain.robinhood.com
# HELIUS_RPC_URL kept for Vercel compatibility — use as custom EVM RPC
HELIUS_RPC_URL=

# ============================================
# TOKEN CONFIGURATION (Required)
# ============================================
# ERC-20 contract address on Robinhood Chain (chain ID 4663)
TOKEN_MINT_ADDRESS=0xYourTokenContractAddress
TOKEN_DECIMALS=18
TOKEN_SYMBOL=TOKEN

# ============================================
# POOL CONFIGURATION
# ============================================
POOL_BALANCE_USD=500
POOL_BALANCE_TOKENS=1000000000

# ============================================
# ELIGIBILITY THRESHOLDS
# ============================================
MIN_TOKEN_HOLDING=1000000
MIN_LOSS_THRESHOLD_PCT=10
MIN_POOL_FOR_PAYOUT=50
MAX_HOLDERS_TO_PROCESS=500

# ============================================
# PAYOUT TIMING
# ============================================
PAYOUT_INTERVAL_MINUTES=5

# Minimum payout pool size in ETH before a cycle executes (default 0.001)
MIN_POOL_ETH=0.001

# ============================================
# PAYOUT EXECUTION
# ============================================
EXECUTE_PAYOUTS=false
# EVM private key (0x-prefixed hex) — same env key name as Solana deployment
PAYOUT_WALLET_PRIVATE_KEY=

# Network: mainnet (4663) or devnet/testnet (46630)
# Same env key name as Solana — values mainnet | devnet
SOLANA_NETWORK=mainnet

# ============================================
# DEV FEE
# ============================================
# Receives the 12% dev fee from each payout (must be 0x EVM address).
# Also excluded from loss-mining rankings/eligibility.
DEV_WALLET_ADDRESS=0xYourDevWalletAddress

# Payout wallet (PAYOUT_WALLET_PRIVATE_KEY) is always excluded from rankings.
# Optional comma-separated extra wallets to exclude from competing:
# EXCLUDED_WALLETS=0xabc...,0xdef...

# ============================================
# SECURITY
# ============================================
CRON_SECRET=your_random_secret_here

# ============================================
# ENVIRONMENT
# ============================================
NODE_ENV=development
